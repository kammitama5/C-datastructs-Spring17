#include <iostream>
#include <cmath>
#include <vector>


using namespace std;

int main(){
	// Binary Search: Rewrite the binary search algorithm from Ch 2 as a template
	// (so this one is required to be a template). So it should search 
	// int arrays, double arrays, string arrays - basically any type of array
	// where the <and> operators are defined 

	// Pre condition -> can take either int array, double array, string array 
	// outputs whether found or not found or index of search (or else -1; ie not found)

	// Post-condition -> returns an int that is the power a ^ n for the values entered 
	
	system("PAUSE");
	return 0;
}

